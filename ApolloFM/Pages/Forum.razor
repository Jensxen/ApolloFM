@page "/forum"
@inject NavigationManager NavigationManager

<div class="sidebar">
    <div class="sidebar-logo">
        <img src="images/ApolloFM.png" alt="ApolloFM Logo" />
    </div>

    <div class="sidebar-menu">
        <NavLink class="sidebar-item" href="dashboard">
            <i class="fas fa-home"></i>
            <span>Dashboard</span>
        </NavLink>

        <NavLink class="sidebar-item" href="forum" Match="NavLinkMatch.All">
            <i class="fas fa-comments"></i>
            <span>Forum</span>
        </NavLink>

        <NavLink class="sidebar-item" href="discover">
            <i class="fas fa-compass"></i>
            <span>Discover</span>
        </NavLink>

        <NavLink class="sidebar-item" href="playlists">
            <i class="fas fa-list"></i>
            <span>Playlists</span>
        </NavLink>

        <NavLink class="sidebar-item" href="profile">
            <i class="fas fa-user"></i>
            <span>Profile</span>
        </NavLink>
    </div>

    <div class="sidebar-footer">
        <button class="logout-btn" @onclick="GoToDashboard">
            <i class="fas fa-arrow-left"></i>
        </button>
    </div>
</div>

<div class="main-content">
    <div class="top-bar">
        <h1>Music Forum</h1>
    </div>

    <div class="content-area">
        <div class="content-card">
            <div class="card-header">
                <div class="d-flex justify-content-between align-items-center">
                    <h3>Recent Discussions</h3>
                    <button class="btn btn-primary">
                        <i class="fas fa-plus me-1"></i> New Topic
                    </button>
                </div>
            </div>
            <div class="card-body">
                <div class="forum-list">
                    @foreach (var topic in forumTopics)
                    {
                        <div class="forum-item">
                            <div class="forum-icon">
                                <i class="@topic.Icon"></i>
                            </div>
                            <div class="forum-details">
                                <h4>@topic.Title</h4>
                                <p>@topic.Description</p>
                                <div class="forum-meta">
                                    <span><i class="fas fa-user me-1"></i> @topic.Author</span>
                                    <span><i class="fas fa-clock me-1"></i> @topic.PostedDate</span>
                                    <span><i class="fas fa-comment me-1"></i> @topic.CommentCount</span>
                                </div>
                            </div>
                            <div class="forum-activity">
                                <div class="activity-indicator @(topic.IsActive ? "active" : "")"></div>
                            </div>
                        </div>
                    }
                </div>
            </div>
        </div>
    </div>
</div>

<style>
    /* Include the same sidebar and general styles as in Dashboard.razor */
    /* Main Layout */
    body {
        background-color: #121212;
        margin: 0;
        padding: 0;
        color: white;
        font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        overflow: hidden;
    }

    /* Sidebar Styles */
    .sidebar {
        position: fixed;
        left: 0;
        top: 0;
        bottom: 0;
        width: 220px;
        background-color: #000000;
        display: flex;
        flex-direction: column;
        border-right: 1px solid #282828;
        z-index: 100;
    }

    .sidebar-logo {
        padding: 20px;
        text-align: center;
    }

        .sidebar-logo img {
            max-width: 140px;
        }

    .sidebar-menu {
        flex: 1;
        padding: 10px 0;
    }

    .sidebar-item {
        display: flex;
        align-items: center;
        padding: 12px 20px;
        color: #b3b3b3;
        text-decoration: none;
        transition: all 0.2s;
        border-left: 3px solid transparent;
    }

        .sidebar-item:hover {
            color: white;
            background-color: #1a1a1a;
        }

        .sidebar-item.active {
            color: white;
            border-left: 3px solid #1db954;
            background-color: #1a1a1a;
        }

        .sidebar-item i {
            width: 24px;
            font-size: 18px;
            margin-right: 12px;
        }

    .sidebar-footer {
        padding: 15px;
        border-top: 1px solid #282828;
        display: flex;
        justify-content: center;
    }

    .logout-btn {
        background: none;
        border: none;
        color: #b3b3b3;
        cursor: pointer;
        padding: 5px;
        transition: color 0.2s;
    }

        .logout-btn:hover {
            color: #1db954;
        }

    /* Main Content Area */
    .main-content {
        margin-left: 220px;
        padding: 20px;
        height: 100vh;
        overflow-y: auto;
    }

    .top-bar {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 30px;
    }

        .top-bar h1 {
            font-size: 28px;
            margin: 0;
            font-weight: 700;
        }

    .content-area {
        padding-bottom: 40px;
    }

    /* Content Card */
    .content-card {
        background-color: #181818;
        border-radius: 8px;
        overflow: hidden;
        box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        margin-bottom: 30px;
    }

    .card-header {
        background-color: #282828;
        padding: 20px;
        border-bottom: 1px solid #333;
    }

        .card-header h3 {
            margin: 0;
            font-size: 20px;
            color: #fff;
            font-weight: 700;
        }

    .card-body {
        padding: 0;
    }

    /* Forum specific styles */
    .forum-list {
        list-style: none;
        margin: 0;
        padding: 0;
    }

    .forum-item {
        display: flex;
        align-items: center;
        padding: 20px;
        border-bottom: 1px solid #282828;
        transition: background-color 0.2s;
        cursor: pointer;
    }

        .forum-item:hover {
            background-color: #282828;
        }

        .forum-item:last-child {
            border-bottom: none;
        }

    .forum-icon {
        width: 50px;
        height: 50px;
        background-color: #333;
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        margin-right: 20px;
        flex-shrink: 0;
    }

        .forum-icon i {
            font-size: 22px;
            color: #1db954;
        }

    .forum-details {
        flex: 1;
    }

        .forum-details h4 {
            margin: 0 0 8px 0;
            font-size: 18px;
            color: #fff;
        }

        .forum-details p {
            margin: 0 0 10px 0;
            color: #b3b3b3;
            font-size: 14px;
            line-height: 1.4;
        }

    .forum-meta {
        display: flex;
        gap: 20px;
    }

        .forum-meta span {
            font-size: 12px;
            color: #b3b3b3;
        }

    .forum-activity {
        margin-left: 15px;
    }

    .activity-indicator {
        width: 10px;
        height: 10px;
        border-radius: 50%;
        background-color: #555;
    }

        .activity-indicator.active {
            background-color: #1db954;
        }

    .btn {
        font-size: 13px;
        padding: 6px 12px;
        border-radius: 4px;
        margin-left: 5px;
    }

    .btn-primary {
        background-color: #1db954;
        border-color: #1db954;
    }

    /* Responsive adjustments */
    @@media (max-width: 768px) {
        .sidebar

    {
        width: 60px;
    }

    .sidebar-item span {
        display: none;
    }

    .sidebar-item {
        justify-content: center;
        padding: 15px 0;
    }

        .sidebar-item i {
            margin-right: 0;
        }

    .sidebar-logo {
        display: none;
    }

    .main-content {
        margin-left: 60px;
    }

    .forum-meta {
        flex-direction: column;
        gap: 5px;
    }

    }
</style>

@code {
    private class ForumTopic
    {
        public string Title { get; set; }
        public string Description { get; set; }
        public string Author { get; set; }
        public string PostedDate { get; set; }
        public int CommentCount { get; set; }
        public bool IsActive { get; set; }
        public string Icon { get; set; }
    }

    private List<ForumTopic> forumTopics = new List<ForumTopic>
    {
        new ForumTopic
        {
            Title = "What's your favorite album of 2023?",
            Description = "Let's discuss everyone's top albums that came out this year. Mine is definitely...",
            Author = "MusicLover42",
            PostedDate = "2 hours ago",
            CommentCount = 24,
            IsActive = true,
            Icon = "fas fa-record-vinyl"
        },
        new ForumTopic
        {
            Title = "Concert experiences thread",
            Description = "Share your best (or worst) concert experiences. I just saw Taylor Swift last weekend and...",
            Author = "ConcertGoer",
            PostedDate = "Yesterday",
            CommentCount = 56,
            IsActive = true,
            Icon = "fas fa-microphone"
        },
        new ForumTopic
        {
            Title = "Looking for indie rock recommendations",
            Description = "I've been listening to the same stuff for months. Can anyone recommend some new indie rock bands?",
            Author = "IndieHead",
            PostedDate = "3 days ago",
            CommentCount = 42,
            IsActive = false,
            Icon = "fas fa-guitar"
        },
        new ForumTopic
        {
            Title = "Vinyl collecting thread",
            Description = "For all the vinyl collectors out there - what are your recent acquisitions? I just picked up...",
            Author = "VinylJunkie",
            PostedDate = "5 days ago",
            CommentCount = 31,
            IsActive = false,
            Icon = "fas fa-compact-disc"
        },
        new ForumTopic
        {
            Title = "Best music production software?",
            Description = "I'm looking to get into music production and would love recommendations on software to start with.",
            Author = "BeginnerProducer",
            PostedDate = "1 week ago",
            CommentCount = 19,
            IsActive = false,
            Icon = "fas fa-sliders-h"
        }
    };

    private void GoToDashboard()
    {
        NavigationManager.NavigateTo("/dashboard");
    }
}
